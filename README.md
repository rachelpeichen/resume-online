# Rachel Chen — Online Resume

Personal resume site built with vanilla HTML/CSS, deployed via Netlify.

## Stack

- Pure HTML5 + CSS (no framework, no build step)
- Hosted on Netlify with continuous deployment from `main`

## Local preview

```bash
open index.html
```

## Deploy

Push to `main` — Netlify auto-deploys.
